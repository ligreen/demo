phantom:
    address: localhost:5000
    load_profile: 
        load_type: rps
        schedule: const(1,1m)
    uris:
      - /
telegraf:
    enabled: false
overload:
  enabled: true
  package: yandextank.plugins.DataUploader
  token_file: "/secret/token.txt"
